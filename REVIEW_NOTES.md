# V1.0 二次复核修订（2026-09-12）

本轮从已交付ZIP重新解压检查；原始基线为 `63b756764a448da5af39b851dddf2cafbdb17f2d`。不是只重跑旧测试，也不是外部真人审计或生产安全认证。

## 已复现并修复的缺口

1. 过期通过证据：原配置版本1不通过、版本2通过、当前版本3再次不通过时，旧实现仍允许用版本2关闭工单。现在在关闭事务内核对目标资产当前版本，必须与所引用的通过结果版本相同。其他资产的变化不误伤该证据。
2. JSON入口一致性：导入文件已有重复键校验，但直接HTTP请求此前可静默采用最后一个重复键。现在对普通JSON、无Content-Type的JSON及+json媒体类型作统一预校验，拒绝重复键、非法常量、错误字符和过深结构，不回显原始请求或口令。
3. CSV解析：严格拒绝错误引号；字段超过CSV解析器容量时返回明确校验错误，而不是未处理异常或静默改写。
4. 并发改密：使用原密码摘要作为原子更新条件；两个持有旧凭据的并发请求只能有一个成功，失败方不能覆盖新密码，也不产生第二条成功审计。
5. Origin：畸形IPv6及含路径的伪Origin得到明确拒绝，不再触发服务异常或被当成合法Origin。
6. CSRF：非ASCII请求令牌明确拒绝，避免直接调用比较函数引发类型异常。

## 验证范围

新增 `tests/test_reaudit.py` 含36个测试实例，包括12组固定随机种子的JSON/CSV往返与规则独立断言。原始应用搭配同一组新增测试得到11失败、25通过；修订后连同原有129项共165项在本地全部通过。原始输入的非法UTF-8被拒绝为400本身不是缺陷，测试接受400或422。

最终GitHub只读工作流将重新checkout本提交，安装依赖、执行全部后端测试和49步真实浏览器流程。应以该次运行的tests.xml、browser/summary.json和源码摘要为准，不以引入回归测试时的失败运行或自动修订任务代替最终验收。

## 使用边界

工单验证必须采用对应资产当前版本的同规则通过结果。改过资产名称、负责人或配置之后也会增加版本，需要新建任务复测；无关资产修改不使原目标证据过期。已关闭工单保留其关闭时的历史依据，之后再次发现不通过应依据新报告另建工单，不自动改写历史结论。

其余边界不变：单机单管理员、配置填报证据核查，不扫描真实设备、不创建虚拟机、不下发策略、不具有外部审计锚定。本轮验证不能替代第三方依赖漏洞评估、Windows实机验证、负载容量测试或著作权权属审查。三份内部Word及申请人信息不上传公开仓库。
